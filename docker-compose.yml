version: '3.3'

services:
  plantuml-server:
    image: plantuml/plantuml-server:jetty
    container_name: plantuml-server
    ports:
      - 8080:8080
    volumes: 
      - ./diagrams/src/:/var/lib/jetty/src